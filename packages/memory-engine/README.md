# @qianling/memory-engine

Platform-neutral persistence for workspace-scoped core facts and archived
conversation documents. Platform adapters provide documents and expose the
small `MemoryStore` interface to their host.

The module deliberately does not import DSH or Kimi packages.
